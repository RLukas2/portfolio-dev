import ShortContent from './short-content';
import ShortHeader from './short-header';

const Short = () => {
  return (
    <div className="relative">
      <ShortHeader />
      <ShortContent />
    </div>
  );
};

export default Short;
